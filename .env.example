# Vercel AI Gateway Plugin Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# AUTHENTICATION (Choose one method)
# =============================================================================

# Method 1: API Key Authentication (Recommended)
# Get from: Vercel Dashboard → AI Gateway → API Keys → Create Key
AI_GATEWAY_API_KEY=your_vercel_api_key_here

# Method 2: OIDC Token Authentication (For Vercel Projects)
# Auto-generated when you run: vercel link && vercel env pull
# VERCEL_OIDC_TOKEN=auto_generated_by_vercel_env_pull

# =============================================================================
# GATEWAY CONFIGURATION
# =============================================================================

# Gateway Base URL (defaults to Vercel AI Gateway)
AIGATEWAY_BASE_URL=https://ai-gateway.vercel.sh/v1

# Alternative gateways:
# AIGATEWAY_BASE_URL=https://openrouter.ai/api/v1  # For OpenRouter
# AIGATEWAY_BASE_URL=https://your-custom-gateway.com/v1  # Custom gateway

# =============================================================================
# MODEL CONFIGURATION (AI SDK format with slash separator)
# =============================================================================

# Default model for small/quick tasks
AIGATEWAY_DEFAULT_MODEL=openai/gpt-4o-mini

# Large model for complex reasoning tasks
AIGATEWAY_LARGE_MODEL=openai/gpt-4o

# Embedding model for semantic search
AIGATEWAY_EMBEDDING_MODEL=openai/text-embedding-3-small

# Alternative model examples:
# AIGATEWAY_DEFAULT_MODEL=anthropic/claude-3-haiku
# AIGATEWAY_LARGE_MODEL=anthropic/claude-3-5-sonnet
# AIGATEWAY_EMBEDDING_MODEL=openai/text-embedding-3-large

# =============================================================================
# PERFORMANCE SETTINGS
# =============================================================================

# Cache TTL in seconds (default: 300 = 5 minutes)
AIGATEWAY_CACHE_TTL=300

# Maximum retry attempts for failed requests (default: 3)
AIGATEWAY_MAX_RETRIES=3

# Request timeout in milliseconds (default: 30000 = 30 seconds)
AIGATEWAY_TIMEOUT=30000

# =============================================================================
# AUTHENTICATION FLAGS
# =============================================================================

# Enable OIDC authentication (set to "true" if using OIDC)
AIGATEWAY_USE_OIDC=false

# =============================================================================
# ELIZAOS CONFIGURATION
# =============================================================================

# Secret salt for ElizaOS (generate with: openssl rand -hex 32)
SECRET_SALT=your_random_32_character_hex_string_here

# Server authentication token (optional, for production)
# ELIZA_SERVER_AUTH_TOKEN=your_server_auth_token_here

# =============================================================================
# OPTIONAL: PLATFORM INTEGRATIONS
# =============================================================================

# Discord Bot Token (if using Discord integration)
# DISCORD_API_TOKEN=your_discord_bot_token_here

# Telegram Bot Token (if using Telegram integration)
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Twitter/X API credentials (if using Twitter integration)
# TWITTER_USERNAME=your_twitter_username
# TWITTER_PASSWORD=your_twitter_password
# TWITTER_EMAIL=your_twitter_email

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# Enable debug mode
# DEBUG=true

# =============================================================================
# QUICK SETUP COMMANDS
# =============================================================================
#
# 1. For API Key Authentication:
#    - Get API key from: https://vercel.com/dashboard → AI Gateway → API Keys
#    - Set AI_GATEWAY_API_KEY above
#    - Set AIGATEWAY_USE_OIDC=false
#
# 2. For OIDC Authentication:
#    - Run: vercel link
#    - Run: vercel env pull
#    - Set AIGATEWAY_USE_OIDC=true
#
# 3. Generate SECRET_SALT:
#    - Run: openssl rand -hex 32
#    - Copy result to SECRET_SALT above
#
# 4. Test your setup:
#    - elizaos start --character examples/basic-character.json
#
# =============================================================================
